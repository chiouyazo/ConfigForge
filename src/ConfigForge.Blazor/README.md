# ConfigForge.Blazor

The ConfigForge Blazor components: the shell, the field controls, and theming. It's an internal package; you normally get it transitively through `ConfigForge.AspNet` or by hosting `ConfigForgeShell` yourself, not by referencing it directly.

Part of [ConfigForge](https://github.com/chiouyazo/ConfigForge).
