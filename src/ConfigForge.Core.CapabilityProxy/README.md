# ConfigForge.Core.CapabilityProxy

Turns calls to a capability interface into HTTP calls against a remote ConfigForge instance, via a `DispatchProxy`. Internal dependency of `ConfigForge.Core`, not intended to be referenced directly.

Part of [ConfigForge](https://github.com/chiouyazo/ConfigForge).
