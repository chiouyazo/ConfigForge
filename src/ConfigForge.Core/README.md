# ConfigForge.Core

The ConfigForge engine: schema parsing, document parse/validate/diff/serialize, document generation, dirty tracking, and the plugin loader. It's an internal package; you normally get it transitively through `ConfigForge.AspNet`, not by referencing it directly.

Part of [ConfigForge](https://github.com/chiouyazo/ConfigForge).
